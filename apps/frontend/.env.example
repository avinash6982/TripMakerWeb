# Frontend Environment Variables

# API URL
# Production (Render): your backend URL or /api if same origin
# Development: http://localhost:3000 (full URL to backend)
VITE_API_URL=http://localhost:3000
