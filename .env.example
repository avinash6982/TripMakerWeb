# TripMaker Monorepo Environment Variables (reference only)
# Backend: use apps/backend/.env.development for local dev (no .env required).
# Frontend: use apps/frontend/.env.development for local dev.
# Production (Render): set all variables in Render Dashboard for each service.

# ===========================================
# Backend (apps/backend/.env.development)
# ===========================================
# PORT=3000
# NODE_ENV=development
# JWT_EXPIRES_IN=7d
# CORS_ORIGINS=http://localhost:5173,http://localhost:4173
# R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME (optional)

# ===========================================
# Frontend (apps/frontend/.env.development)
# ===========================================
# VITE_API_URL=http://localhost:3000

# ===========================================
# Production (Render Dashboard)
# ===========================================
# Backend service: JWT_SECRET (required), NODE_ENV=production, CORS_ORIGINS, R2_* (optional).
# Frontend: VITE_API_URL = your backend API URL (e.g. https://your-backend.onrender.com).
