# TripMaker Monorepo Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# Backend Configuration (apps/backend/)
# ===========================================

# Server Port (for local development)
PORT=3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production
JWT_EXPIRES_IN=7d

# Database Path (optional - defaults to data/users.json)
# USER_DB_PATH=./data/users.json

# CORS Origins (comma-separated list, use * for all origins in dev)
CORS_ORIGINS=http://localhost:5173,http://localhost:4173

# Node Environment
NODE_ENV=development

# ===========================================
# Frontend Configuration (apps/frontend/)
# ===========================================

# API URL for frontend
# In production (Vercel), use /api (relative path)
# In development, use full URL
VITE_API_URL=http://localhost:3000

# ===========================================
# Production-specific (Vercel)
# ===========================================
# In Vercel Dashboard, set:
# - JWT_SECRET (required)
# - NODE_ENV=production
# - VITE_API_URL=/api (for frontend)
# - CORS_ORIGINS=https://your-app.vercel.app
